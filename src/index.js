import React from 'react';
import ReactDOM from 'react-dom/client';
import AppProfile from './components/App.js';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
    <AppProfile />
  </React.StrictMode>
);

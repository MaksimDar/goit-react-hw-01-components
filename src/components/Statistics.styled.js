import styled from 'styled-components';

const MainInfo = styled.span`
  font-size: 18px;
  line-height: 1.5;
  color: orange;
`;

const StatisticSection = styled.section`
  list-style: none;
  text-align: center;
`;
export { MainInfo, StatisticSection };
